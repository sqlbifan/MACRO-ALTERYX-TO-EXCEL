"transform": [
    { "calculate": "datum.SORT == 1 ? datum.SCORE_TEXT : null", "as": "SCORE_TEXT_SORT1" },
    { "aggregate": [ { "op": "max", "field": "SCORE_TEXT_SORT1", "as": "SCORE_TEXT_SELECTED" } ] }
  ],
